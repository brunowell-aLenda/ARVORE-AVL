#ifndef MENU_H_INCLUDED
#define MENU_H_INCLUDED

int Inteiro(void); // lê inteiro
int Inteiro_CPF(void); // le inteiro cpf
float Float(void); // lê float
char* String(void);// lê string
void limpa_buffer(void); // limpa_buffer
char* Camelcase(char* strings);
char Valida_Sexo(void);


#endif // MENU_H_INCLUDED
